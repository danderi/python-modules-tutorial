# 🔧 This is a helper module that our entry point will use
# It contains simple functions that we can import and use in our main program

def greet(name):
    """A simple function to greet someone"""
    print(f"Hello, {name}!")

# We don't need a main block here because this is a helper module
# It's meant to be imported, not run directly