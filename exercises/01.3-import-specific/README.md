# 🎯 Import Specific Parts

Sometimes you don't need everything from a module - you just want one or two things! Let's learn how to import specific items. 

## 📝 Instructions

1. From the `random` module, import only the `choice` function
2. Create a list of fruits: "apple", "banana", "orange", "grape"
3. Use the `choice()` function to randomly select and print a fruit

## 💡 Hint

To import just one thing from a module, use:
